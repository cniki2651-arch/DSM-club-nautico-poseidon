package com.dsm.clubnauticoposeidon.ui.theme

import androidx.compose.ui.graphics.Color

// Azules marinos (fondo y superficies)
val Navy900 = Color(0xFF040D1F)   // Fondo principal de la pantalla
val Navy800 = Color(0xFF081832)   // Fondo de tarjetas/sheet inferior
val Navy700 = Color(0xFF0D2547)   // Superficies secundarias
val Navy600 = Color(0xFF133460)   // Bordes o superficies más claras

// Dorados
val Gold300 = Color(0xFFF5E0A3)   // Dorado más claro (brillo, detalle del ícono ancla)
val Gold400 = Color(0xFFE8C775)   // Dorado medio-claro (labels, links, texto "Marina & Yacht Club")
val Gold500 = Color(0xFFC9A24D)   // Dorado principal (botón, bordes de foco)
val Gold600 = Color(0xFFA8843A)   // Dorado oscuro (sombra/degradé del botón)

// Textos y neutros
val Ink = Color(0xFFEAF1FB)       // Texto principal claro (títulos, inputs)
val Muted = Color(0xFF8AA2C4)     // Texto secundario/placeholder (subtítulos, iconos inactivos)

val ErrorRed = Color(0xFFE57373)